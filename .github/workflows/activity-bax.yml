name: Activity Box

#on:
#  schedule:
#    - cron: '*/10 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - uses: FlorianSebille/activity-box@master
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_USERNAME: FlorianSebille
          GIST_ID: 031167901a55328a24273ed3e18f95f3